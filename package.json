{
  "name": "acculab",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "astro preview",
    "verify:precios": "node scripts/verificar-precios.mjs",
    "qa": "node scripts/qa-crawl.mjs",
    "validar:schema": "bash scripts/validar-schema.sh"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "astro": "^7.3.5",
    "satori": "^0.33.5",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "node-html-parser": "^9.0.4"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
